<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "YOMOVIES - Watch Latest Movies & TV Shows Online" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="YOMOVIES Clone">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 text-white">
    <!-- Header -->
    <header class="bg-gray-800 sticky top-0 z-50">
      <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <div class="flex items-center space-x-10">
          <%= link_to "YOMOVIES", root_path, class: "text-2xl font-bold text-red-600" %>
          
          <nav class="hidden md:flex space-x-6">
            <%= link_to "Movies", movies_path, class: "hover:text-red-500 transition" %>
            <%= link_to "TV Shows", tv_shows_path, class: "hover:text-red-500 transition" %>
            <% Genre.all.limit(5).each do |genre| %>
              <%= link_to genre.name, genre_path(genre), class: "hover:text-red-500 transition" %>
            <% end %>
          </nav>
        </div>
        
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <span class="mr-4">Welcome, <%= current_user.email.split('@').first %></span>
            <%= link_to "Profile", user_path(current_user), class: "hover:text-red-500 transition" %>
            <% if current_user.admin? %>
              <%= link_to "Admin Panel", admin_movies_path, class: "hover:text-red-500 transition" %>
            <% end %>
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }  , class: "hover:text-red-500 transition" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "hover:text-red-500 transition" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "hover:text-red-500 transition ml-4" %>
          <% end %>
        </div>
      </div>
      
      <!-- Search Bar -->
      <div class="container mx-auto px-4 py-2">
        <%= form_with url: search_path, method: :get, local: true, class: "flex" do |form| %>
          <%= form.text_field :q, placeholder: "Search for movies, TV shows...", 
                              value: params[:q], 
                              class: "w-full px-4 py-2 rounded-l text-gray-800" %>
          <%= form.submit "Search", class: "bg-red-600 px-4 py-2 rounded-r cursor-pointer hover:bg-red-700" %>
        <% end %>
      </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-6">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4 text-black">
          <%= notice %>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 text-black">
          <%= alert %>
        </div>
      <% end %>
      
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <footer class="bg-gray-800 py-8 mt-10">
      <div class="container mx-auto px-4 text-center">
        <p>&copy; 2026 YOMOVIES Clone. All rights reserved.</p>
        <p class="mt-2 text-gray-400">Watch the latest movies and TV shows online for free.</p>
      </div>
    </footer>
  </body>
</html>
